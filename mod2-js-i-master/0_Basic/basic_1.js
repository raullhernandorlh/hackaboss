/**
 * Esta es la estructura típica de un fichero de código. Distinguiréis
 * dos partes bien diferenciadas:
 *    - los comentarios, que usan para añadir explicaciones y no serán
 *      interpretados por el ordenador. Este texto es un ejemplo de comentario
 *    - el código, es decir, las instrucciones que implementan la funcionalidad
 *      que nos interesa y que el ordenador interpretará
 * 
 * Una aplicación puede estar formada por uno o más ficheros, para ejecutarlo basta
 * con escribir `node <nombre del fichero>` en la consola, en este caso `node ex_0.js`
 * 
 * La aplicación implementada en este fichero es muy simple: escribe en pantalla la cadena
 * `Hola mundo!`
 * 
 * Notas:
 *  - recordad guardar el fichero que estéis escribiendo antes de ejecutar. Si no lo hacéis,
 *    el sistema no será consciente de los últimos cambios añadidos.
 * 
 */

 console.log('Hola mundo!');
